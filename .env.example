# =============================================================================
# AVD Demo - Master Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# This single file feeds ALL deployment steps:
#   1. Terraform ENVSetup  (Compute Gallery + Image Definition)
#   2. Packer Image Builds (Custom Windows images)
#   3. Terraform AVD       (Host Pools, Session Hosts, Networking)
# =============================================================================

# -----------------------------------------------------------------------------
# Azure Identity
# -----------------------------------------------------------------------------
# Your Azure subscription ID (find with: az account show --query id -o tsv)
AZ_SUBSCRIPTION_ID=00000000-0000-0000-0000-000000000000

# Azure region for all resources
AZ_LOCATION=centralus

# Azure cloud environment: Public | USGovernment
CLOUD_ENVIRONMENT=Public

# -----------------------------------------------------------------------------
# Compute Gallery
# -----------------------------------------------------------------------------
# Resource group that holds the Compute Gallery (created by ENVSetup)
GALLERY_RG=AVD_Demo_Resources

# Name of the Azure Compute Gallery
GALLERY_NAME=mclab_avd_images

# Description for the gallery
GALLERY_DESCRIPTION=Customized Windows Client Images for AVD Demos

# Regions to replicate images to (JSON array)
REPLICATION_REGIONS=["centralus"]

# -----------------------------------------------------------------------------
# Image Definition
# -----------------------------------------------------------------------------
# Which image template to build: dev | data
IMAGE_TYPE=dev

# Name of the image definition in the gallery (e.g. "developer", "data-analyst")
IMAGE_NAME=developer

# OS type for the image definition
IMAGE_OS_TYPE=Windows

# Hyper-V generation (V1 or V2) — V2 required for modern Windows 11 images
IMAGE_HYPER_V_GEN=V2

# Custom image identifier metadata
IMAGE_PUBLISHER=MCLabEnterprises
IMAGE_OFFER=Developer
IMAGE_SKU=AVD-Dev

# -----------------------------------------------------------------------------
# Build VM (Packer)
# -----------------------------------------------------------------------------
# VM size for the Packer build VM
BUILD_VM_SIZE=Standard_D2s_v3

# OS disk size in GB for the build VM
BUILD_VM_DISK_SIZE=128

# Source marketplace image for Packer to start from
SOURCE_IMAGE_PUBLISHER=microsoftwindowsdesktop
SOURCE_IMAGE_OFFER=windows-11
SOURCE_IMAGE_SKU=win11-24h2-ent

# -----------------------------------------------------------------------------
# AVD Session Hosts
# -----------------------------------------------------------------------------
# VM size for AVD session host VMs
SESSION_HOST_SIZE=Standard_D4s_v5

# Local admin username on session hosts
LOCAL_ADMIN=shadmin

# -----------------------------------------------------------------------------
# Networking (AVD)
# -----------------------------------------------------------------------------
# VNet address space (JSON array)
VNET_ADDRESS_SPACE=["10.12.0.0/16"]

# Subnet name and prefix for session host VMs
SUBNET_NAME=vm
SUBNET_PREFIX=["10.12.1.0/24"]

# -----------------------------------------------------------------------------
# Tags
# -----------------------------------------------------------------------------
TAG_ENVIRONMENT=demo
TAG_PROJECT=avd-custom-images
